#!/usr/bin/python3
""" checks instance of object against class and subclass """


def is_kind_of_class(obj, a_class):
    """ the function that does the checking """
    return isinstance(obj, a_class)
